# JamShield - Mosquitto WebSocket listener (for the PWA / browser MQTT clients).
# Installed to /etc/mosquitto/conf.d/jamshield-ws.conf by rpi_iot_setup.sh.
# The plain TCP listener (1883, for the ESP32) is in jamshield.conf.

listener 9001 0.0.0.0
protocol websockets
allow_anonymous true
